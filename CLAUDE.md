# CLAUDE.md — 名刺管理アプリ 開発ガイド

## 1. 技術スタック

| 技術 | 詳細 |
|---|---|
| マークアップ | HTML5 |
| スクリプト | JavaScript（Vanilla、フレームワーク不使用） |
| スタイル | Tailwind CSS（CDN 読み込み） + styles.css（カスタム補足） |
| 永続化 | localStorage（サーバー・DB・外部API は一切使わない） |
| ビルド | なし（npm / バンドラー 禁止） |

---

## 2. ディレクトリ構成

```
名刺【section4-4】/
├── index.html   # HTML骨格・CDN読み込み・ペイン定義
├── app.js       # 全ロジック（CRUD・検索・画面切替）
└── styles.css   # Tailwind で表現しきれないカスタムスタイル
```

### 各ファイルの責務

- **index.html** — 構造のみ。スクリプトの直書き禁止。
- **app.js** — 即時実行関数（IIFE）で全体を包み、グローバル汚染を防ぐ。
- **styles.css** — アクセントカラー変数、カードホバー、選択ハイライトなど Tailwind ユーティリティで完結しない部分だけ書く。

---

## 3. コーディング規約

### 画面切替方式

右ペインは3つのサブ要素を持ち、表示/非表示を切り替える。

```
#pane-empty   — 何も選んでいない初期状態
#pane-detail  — 名刺の詳細表示
#pane-form    — 登録・編集フォーム
```

切替は専用関数 `showPane(paneName)` 一本で行う。直接 `display` を書き換えない。

### ID 命名規則

- 右ペイン要素: `pane-*`（`pane-empty` / `pane-detail` / `pane-form`）
- 入力フィールド: `field-*`（`field-name` / `field-company` など）
- ボタン: `btn-*`（`btn-new` / `btn-save` / `btn-edit` / `btn-delete`）
- 検索ボックス: `search-input`
- 名刺リスト: `card-list`

### 関数の長さ

- 1関数は **50行以内** を原則とする。超える場合はサブ関数に分割する。

### 変数宣言

- `const` を優先。再代入が必要な場合のみ `let`。`var` は使わない。

### グローバル汚染禁止

```js
(function () {
  'use strict';
  // アプリ全体のコード
})();
```

### コメント方針

- 「何をしているか」ではなく「なぜそうしているか」だけコメントする。
- 関数名・変数名で自明な処理にコメントは不要。

---

## 4. データ構造

### 名刺1件のオブジェクト

```js
{
  id:        "1717000000000",   // Date.now() の文字列
  name:      "山田 太郎",        // 必須
  company:   "株式会社サンプル",  // 必須
  title:     "営業部長",         // 任意
  email:     "yamada@example.com", // 任意
  phone:     "03-0000-0000",    // 任意
  memo:      "展示会で名刺交換", // 任意・複数行可
  createdAt: "2024-01-01T00:00:00.000Z" // ISO文字列・自動付与
}
```

### localStorage

- キー: `meishi-cards`
- 値: 上記オブジェクトの **配列** を `JSON.stringify` した文字列
- 読み込み: `JSON.parse(localStorage.getItem('meishi-cards') || '[]')`

---

## 5. デザイン規約

| 項目 | 値 |
|---|---|
| アクセントカラー | `#c15f3c`（Claudeオレンジ） |
| フォント | `"游ゴシック", "Yu Gothic", sans-serif` |
| 左ペイン幅 | `340px` 固定 |
| 角丸 | `rounded-lg`（8px）で統一 |
| 影 | `shadow-sm` 程度（控えめ） |
| 並び順 | `createdAt` の新しい順（降順） |
| ベース背景 | 白（`#ffffff`） |
| サブ背景 | 薄グレー（`#f9fafb`） |

### ボタンスタイル

- **主要アクション**（保存・新規登録）: アクセントカラー背景 + 白テキスト
- **危険アクション**（削除）: `red-500` 背景 + 白テキスト
- **サブアクション**（編集・キャンセル）: ボーダーのみ + グレーテキスト

---

## 6. やってはいけないこと

| 禁止事項 | 理由 |
|---|---|
| `npm` / `node_modules` の使用 | ビルドレス構成を維持するため |
| Webpack / Vite 等のバンドラー | 同上 |
| サーバーサイドコード（Node.js, Python 等） | 純粋なフロントエンドアプリのため |
| 外部API呼び出し（fetch / XHR で外部サーバーへ） | オフライン動作・プライバシー保護のため |
| `localStorage` 以外へのデータ保存 | 技術制約のため |
| グローバルスコープへの関数・変数の直接定義 | 即時実行関数で包むこと |
| `var` の使用 | `const` / `let` を使うこと |
| OCR・ログイン・クラウド同期の実装 | 今回のスコープ外 |

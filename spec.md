# spec.md — 名刺管理アプリ 仕様書

## 1. 目的・利用シーン

### 目的

紙の名刺を保管する代わりに、ブラウザ1枚でデジタル管理できる **ひとり用の名刺管理アプリ**。  
業務用 SaaS の核となる CRUD 機能（OCR を除く）を最小構成で再現する。

### 利用シーン

- 展示会・商談後に受け取った名刺をその場でブラウザに入力して保存する
- 後から名前や会社名で素早く検索して連絡先を確認する
- メモ欄に「いつ・どこで会ったか」を記録しておく

---

## 2. データ項目

| 項目 | キー | 型 | 必須 | 備考 |
|---|---|---|---|---|
| ID | `id` | string | ○ | `Date.now()` の文字列。自動採番 |
| 氏名 | `name` | string | ○ | 例: 山田 太郎 |
| 会社名 | `company` | string | ○ | 例: 株式会社サンプル |
| 役職 | `title` | string |  | 任意 |
| メール | `email` | string |  | 任意 |
| 電話 | `phone` | string |  | 任意 |
| メモ | `memo` | string |  | 任意・複数行可 |
| 作成日時 | `createdAt` | string | ○ | ISO 8601 文字列。自動付与 |

---

## 3. 画面構成

### 全体レイアウト

ツーカラム構成（PC表示専用。モバイル対応は対象外）。

```
┌──────────────────────┬──────────────────────────────────────┐
│  左ペイン（340px固定）  │  右ペイン（flex-1）                    │
│                      │                                      │
│  [検索ボックス]        │  ← モード切替（下記3パターン）           │
│  [＋ 新規登録]        │                                      │
│  ─────────────────   │                                      │
│  名刺カードリスト       │                                      │
│  （選択中はハイライト）  │                                      │
└──────────────────────┴──────────────────────────────────────┘
```

### 左ペイン

| 要素 | 説明 |
|---|---|
| 検索ボックス | 入力のたびにリアルタイムで絞り込み |
| 新規登録ボタン | クリックで右ペインをフォームモードに切替 |
| 名刺カードリスト | 作成日時の新しい順に表示。カードには氏名・会社名・役職を表示 |

### 右ペイン（3モード）

| モード | ID | 表示タイミング |
|---|---|---|
| 空状態 | `pane-empty` | 起動直後・削除後・選択解除後 |
| 詳細 | `pane-detail` | 名刺カードを選択したとき |
| フォーム | `pane-form` | 新規登録ボタン押下時・編集ボタン押下時 |

---

## 4. CRUD 操作フロー

| 操作 | 起点 | 遷移 | 結果 |
|---|---|---|---|
| **Create** | 新規登録ボタン | 空状態 → フォーム（空）→ 保存 | 詳細モードへ。リストの先頭に追加 |
| **Read（一覧）** | アプリ起動 | — | 左ペインに名刺リストを表示 |
| **Read（検索）** | 検索ボックス入力 | — | 氏名・会社名・役職で横断絞り込み |
| **Update** | 詳細の編集ボタン | 詳細 → フォーム（既存値入り）→ 保存 | 詳細モードに戻る。リストも更新 |
| **Delete** | 詳細の削除ボタン | 詳細 → 確認ダイアログ → OK | 空状態に戻る。リストから削除 |

---

## 5. 永続化方針

| 項目 | 内容 |
|---|---|
| 保存先 | `localStorage` |
| キー | `meishi-cards` |
| 保存形式 | 名刺オブジェクトの配列を `JSON.stringify` した文字列 |
| 読み込み | `JSON.parse(localStorage.getItem('meishi-cards') \|\| '[]')` |
| 更新タイミング | 登録・編集・削除のたびに配列全体を上書き保存 |

---

## 6. 初期データ

- `meishi-cards` キーが存在しない（`null`）場合のみ、起動時にサンプル3件を自動投入する。
- サンプルデータは**実在しない**人物名・会社名を使用する。
- 一度でも保存操作が行われた後は再投入しない。

### サンプルデータ例

| 氏名 | 会社名 | 役職 |
|---|---|---|
| 佐藤 翔太 | 株式会社フューチャーブリッジ | 代表取締役 |
| 鈴木 莉奈 | ネクストウェーブ合同会社 | マーケティング部長 |
| 高橋 健一 | グローバルテック株式会社 | シニアエンジニア |

---

## 7. 対象外の機能

以下は今回のスコープに含まない。

| 機能 | 理由 |
|---|---|
| OCR（画像からの自動入力） | 外部API が必要なため |
| クラウド保存・他端末共有 | サーバーレス構成のため |
| ログイン・マルチユーザー | ひとり用アプリのため |
| インポート／エクスポート（CSV等） | 最小構成を維持するため |
| モバイル対応（レスポンシブ） | PC専用のため |
| データバックアップ・復元 | スコープ外 |
